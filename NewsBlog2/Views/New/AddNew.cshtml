@model EntityLayer.Concrete.New

@{
    ViewData["Title"] = "AddNew";
}

@using (Html.BeginForm("AddNew", "New", FormMethod.Post))
{
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <p class="card-description"> Yeni Category Ekleme Paneli </p>
                <form class="forms-sample">
                    <div class="form-group">
                        <label>Başlık</label>
                        <input type="text" name="Title" class="form-control">
                    </div>
                    <div class="form-group">
                        <label>İçerik</label>
                        <input type="text" name="Description" class="form-control">
                    </div>
                    <div class="form-group">
                        <label>Resim Yolu</label>
                        <input type="text" name="ImageURL" class="form-control">
                    </div>
                    @*<div class="form-group">
                        <label>Kategori</label>
                        @Html.DropDownListFor(m => m.CategoryID, (SelectList)ViewBag.CategoryList, "Lütfen Seçiniz", new { @class = "dropdown form-control" }))
                    </div>*@
                    <button type="submit" class="btn btn-primary mr-2">Kaydet</button>
                    <button class="btn btn-dark">İptal</button>
                </form>
            </div>
        </div>
    </div>
}

